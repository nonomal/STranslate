<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:control="clr-namespace:STranslate.Controls"
    xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern">

    <Style TargetType="{x:Type control:Badge}">
        <Setter Property="FontSize" Value="{DynamicResource STControlFontSize12}" />
        <Setter Property="Padding" Value="3" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Foreground" Value="{DynamicResource TextFillColorInverseBrush}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type control:Badge}">
                    <Border
                        x:Name="PART_Border"
                        Height="{TemplateBinding Height}"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="{TemplateBinding CornerRadius}">
                        <TextBlock
                            x:Name="PART_TextBlock"
                            Padding="{TemplateBinding Padding}"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            FontSize="{TemplateBinding FontSize}"
                            FontWeight="{TemplateBinding FontWeight}"
                            Foreground="{TemplateBinding Foreground}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsOfficial" Value="True">
                            <Setter TargetName="PART_TextBlock" Property="Text" Value="{DynamicResource Official}" />
                            <Setter TargetName="PART_Border" Property="Background" Value="{DynamicResource AccentTextFillColorTertiaryBrush}" />
                        </Trigger>
                        <Trigger Property="IsOfficial" Value="False">
                            <Setter TargetName="PART_TextBlock" Property="Text" Value="{DynamicResource Plugins}" />
                            <Setter TargetName="PART_Border" Property="Background" Value="{DynamicResource SystemFillColorCautionBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>